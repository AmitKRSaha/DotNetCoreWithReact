import React, { Component } from 'react';
import './Card.css';
import { Card } from './Card';

export const CardList = (props) => {
    return (
        <div>
            <Card />
        </div>
    );
};